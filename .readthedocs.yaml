version: 2
build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  commands:
    - pipx install "poetry>=2.3,<3"
    - poetry install --with docs
    - poetry run mkdocs build -d $READTHEDOCS_OUTPUT/html
