{
    "ignorePatterns": [
        {
            "pattern": "^https://github.com"
        },
        {
            "pattern": "^http://path/to/some/"
        },
        {
            "pattern": "^http://host"
        },
        {
            "pattern": "^http://nbviewer.ipython.org/"
        },
        {
            "pattern": "^http://some/file/from/"
        }
    ]
}
